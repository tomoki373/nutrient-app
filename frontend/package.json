{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0 --port $PORT",
    "start": "npm run preview"
  },
    "dependencies": {
      "vite": "^8.0.10"
    },
    "devDependencies": {
      "typescript": "~6.0.2"
    }
}
